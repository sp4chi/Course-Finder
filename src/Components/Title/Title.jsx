import { Typography } from "@mui/material";

const Title = () => {
    return (<Typography variant="h2" fontFamily='Monospace' sx={{
        color: 'error.main',
        mt: 5,
        mb: 3
    }} >
        Welcome to Course - Finder
    </Typography >);
}

export default Title;