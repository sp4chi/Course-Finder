import { Checkbox, FormControlLabel } from "@mui/material";


export default function CheckboxLabels() {
    return (
        < FormControlLabel control={< Checkbox defaultChecked />} label="Self Paced" />
    );
}
